import 'package:flutter/material.dart';

class CampStatusScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Camp Status')),
      body: Center(
        child: Text('Camp Status Screen'),
      ),
    );
  }
}
